import { type GraphState } from '../state/GraphState';

/**
 * Placeholder node from previous implementation phases.
 */
export const synthesisNode = async (_state: GraphState): Promise<Partial<GraphState>> => {
  return {};
};
